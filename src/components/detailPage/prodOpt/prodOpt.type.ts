export interface PropsType {
  drop?: boolean;
  option?: [string | null, string | null, string | null, number, number][];
  price?: number;
  discount?: number;
  like?: boolean;
  likeCnt?: number;
  colorCode?: string;
}

import * as t from "./AsideHeader.style";
import { useState } from "react";

interface DataType {
  isOpen: boolean;
  handleClose: () => void;
}

const AsideHeader = (props: DataType) => {
  return <></>;
};

export default AsideHeader;
